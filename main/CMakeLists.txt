# The following lines of boilerplate have to be in your project's CMakeLists.txt
cmake_minimum_required(VERSION 3.16)

# This includes the ESP-IDF build system logic
include($ENV{IDF_PATH}/tools/cmake/project.cmake)

# Name your project here (this will show up in your build folder)
project(anti-sleep-system)
